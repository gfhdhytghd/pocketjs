[package]
name = "pocket-mod"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/pocket-stack/pocketjs"
description = "Guest hosting for the Pocket runtime family: one QuickJS realm, mounted surfaces, one guest turn per tick (docs/RUNTIMES.md)"

[dependencies]
pocketjs-core = { path = "../../core", features = ["std"] }
rquickjs = "0.12"
anyhow = "1"
log = "0.4"
